resources:
  pipelines:
    floodgate_pipeline:
      name: floodgate_pipeline
      root_path: ../../..
      configuration:
        bundle.catalog: "${var.catalog}"
        bundle.bronze_schema: "${var.bronze_schema}"
        bundle.silver_schema: "${var.silver_schema}"
        bundle.gold_schema: "${var.gold_schema}"
      libraries:
        - glob:
            include: ../../pipelines/floodgate_tables.py
      photon: true
      catalog: "${var.catalog}"
      schema: "${var.silver_schema}"
      serverless: true
      continuous: false
      environment:
        dependencies: ${var.wheel_dependencies}