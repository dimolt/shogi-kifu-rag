name: CI

on:
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup dependencies
        uses: ./.github/workflows/setup-deps.yml
        with:
          groups: '--group pyspark --group devTools'
      
      - name: Run unit tests
        run: |
          uv run pytest tests/ -v

      - name: Run Ruff
        run: |
          uv run ruff check .

      - name: Run mypy
        run: |
          uv run mypy src/ --exclude 'tests'
