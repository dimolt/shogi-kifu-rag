[project]
name = "ShogiApp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12,<3.13"

dependencies = [
    "python-shogi",
    "pydantic",
    "pydantic-settings",
    "python-dotenv",
    "chardet>=7.4.3",
]

[dependency-groups]
pyspark = [
    "pyspark",
]

dbx = [
    "databricks-connect>=15.4,<15.5",
    "chromadb",
    "sentence-transformers",
    "requests",
    "beautifulsoup4",
    "gradio",
    "google-generativeai",
    "groq",
    "databricks-sdk",
]

devTools = [
    "pytest",
    "ruff",
    "mypy",
    "ipykernel",
]

[tool.uv]
package = true

[tool.setuptools]
package-dir = {"" = "code/remote/src"}

[tool.setuptools.packages.find]
where = ["code/remote/src"]

[project.scripts]
chromadb = "shogi_app.jobs.chromadb:main"
floodgate = "shogi_app.jobs.floodgate:main"
wikipedia = "shogi_app.jobs.wikipedia:main"
